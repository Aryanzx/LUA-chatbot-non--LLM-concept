# System Architecture Diagram

## Overall System Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                         TRAINING PHASE                              │
│                    (Python + PyTorch + GPU)                         │
└─────────────────────────────────────────────────────────────────────┘
                                 │
                                 │
                    ┌────────────┴────────────┐
                    │                         │
            ┌───────▼─────┐          ┌───────▼────────┐
            │  Load HF    │          │  Initialize    │
            │  Dataset    │          │  Model         │
            └───────┬─────┘          └───────┬────────┘
                    │                        │
                    └────────┬───────────────┘
                             │
                    ┌────────▼─────────┐
                    │   Training Loop  │
                    │  - Forward Pass  │
                    │  - Compute Loss  │
                    │  - Backprop      │
                    │  - Update Params │
                    └────────┬─────────┘
                             │
                    ┌────────▼─────────┐
                    │  Export Model    │
                    │  - config.json   │
                    │  - *.npy weights │
                    └────────┬─────────┘
                             │
                             │ Transfer
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        INFERENCE PHASE                              │
│                         (Lua + CPU)                                 │
└─────────────────────────────────────────────────────────────────────┘
                             │
                    ┌────────▼─────────┐
                    │  Load Weights    │
                    │  - Parse NPY     │
                    │  - Build Model   │
                    └────────┬─────────┘
                             │
                    ┌────────▼─────────┐
                    │   Interactive    │
                    │   Generation     │
                    │  - User Input    │
                    │  - Forward Pass  │
                    │  - Top-K Sample  │
                    └──────────────────┘
```

## Detailed Model Architecture

```
                         INPUT TOKENS
                              │
                              ▼
                    ┌─────────────────┐
                    │ Token Embedding │
                    │        +        │
                    │Position Embedding│
                    └────────┬────────┘
                             │
         ┌───────────────────┼───────────────────┐
         │                   │                   │
         ▼                   ▼                   ▼
    ┌────────┐          ┌────────┐          ┌────────┐
    │ Layer 1│          │ Layer 2│          │ Layer 3│
    └────┬───┘          └────┬───┘          └────┬───┘
         │                   │                   │
         │    ┌──────────────┴──────────────┐    │
         │    │                             │    │
         ▼    ▼                             ▼    ▼
    ┌─────────────┐                   ┌─────────────┐
    │ ENGRAM      │                   │ ENGRAM      │
    │ MEMORY      │                   │ MEMORY      │
    │ (Layer 1)   │                   │ (Layer 3)   │
    └──────┬──────┘                   └──────┬──────┘
           │                                 │
           └──────────────┬──────────────────┘
                          │
                          ▼
                 ┌────────────────┐
                 │ Output Layer   │
                 │ (Vocabulary)   │
                 └────────┬───────┘
                          │
                          ▼
                   ┌──────────────┐
                   │ MARKOV CHAIN │
                   │  THINKING    │
                   └──────┬───────┘
                          │
                          ▼
                   ┌──────────────┐
                   │  TOP-K       │
                   │  SAMPLING    │
                   └──────┬───────┘
                          │
                          ▼
                   GENERATED TOKEN
```

## Single Transformer Layer Detail

```
                     Input: h[t]
                          │
              ┌───────────┴───────────┐
              │                       │
              ▼                       │
      ┌──────────────┐               │
      │ Self-        │               │
      │ Attention    │               │
      └──────┬───────┘               │
              │                       │
              │ Residual              │
              ▼                       │
      ┌──────────────┐               │
      │ Add & Norm   │◄──────────────┘
      └──────┬───────┘
              │
              │
    ┌─────────┴─────────┐
    │                   │
    │   Apply Engram?   │
    │                   │
    └─────────┬─────────┘
              │ YES (if Engram layer)
              ▼
    ┌─────────────────────┐
    │  ENGRAM MODULE      │
    │                     │
    │  ┌──────────────┐   │
    │  │  Retrieve    │   │
    │  │  2-grams     │   │
    │  └──────┬───────┘   │
    │         │           │
    │  ┌──────▼───────┐   │
    │  │  Retrieve    │   │
    │  │  3-grams     │   │
    │  └──────┬───────┘   │
    │         │           │
    │  ┌──────▼───────┐   │
    │  │  Gate &      │   │
    │  │  Fuse        │   │
    │  └──────────────┘   │
    └─────────┬───────────┘
              │
              ▼
      ┌──────────────┐
      │ Add & Norm   │
      └──────┬───────┘
              │
              ▼
      ┌──────────────┐
      │   MoE or     │
      │   FFN        │
      └──────┬───────┘
              │
              │ Residual
              ▼
      ┌──────────────┐
      │ Add & Norm   │
      └──────┬───────┘
              │
              ▼
           Output: h[t+1]
```

## Engram Memory Module (Detailed)

```
PHASE 1: RETRIEVAL
==================

Input Tokens: [x[t-2], x[t-1], x[t]]
              │
              ▼
       ┌──────────────┐
       │ Compress     │
       │ Vocabulary   │
       └──────┬───────┘
              │
              ▼
    Extract N-grams
              │
    ┌─────────┴─────────┐
    │                   │
    ▼                   ▼
┌────────┐          ┌────────┐
│2-gram: │          │3-gram: │
│(t-1,t) │          │(t-2,   │
│        │          │ t-1,t) │
└────┬───┘          └────┬───┘
     │                   │
     ▼                   ▼
┌─────────┐         ┌─────────┐
│ Hash    │         │ Hash    │
│Function │         │Function │
└────┬────┘         └────┬────┘
     │                   │
     ▼                   ▼
┌─────────┐         ┌─────────┐
│Embedding│         │Embedding│
│Table E2 │         │Table E3 │
│10K rows │         │50K rows │
└────┬────┘         └────┬────┘
     │                   │
     └─────────┬─────────┘
               │
               ▼
         ┌──────────┐
         │ Concat   │
         │ [e2; e3] │
         └────┬─────┘
              │
              ▼
       ┌──────────────┐
       │ Linear       │
       │ Projection   │
       └──────┬───────┘
              │
              ▼
         Retrieved: ê[t]


PHASE 2: FUSION
===============

Retrieved ê[t]         Hidden h[t]
      │                    │
      │    ┌───────────────┘
      │    │
      ▼    ▼
  ┌──────────────┐
  │  RMS Norm    │
  │  both inputs │
  └──────┬───────┘
         │
         ▼
  ┌──────────────┐
  │ Compute Gate │
  │ α = σ(h·W_k·ê)│
  └──────┬───────┘
         │
         ▼
  ┌──────────────┐
  │  Modulate    │
  │  u = α·W_v·ê │
  └──────┬───────┘
         │
         ▼
  ┌──────────────┐
  │ 1D Conv      │
  │ (refine)     │
  └──────┬───────┘
         │
         ▼
  ┌──────────────┐
  │ Add to       │
  │ Hidden State │
  │ h' = h + u   │
  └──────────────┘
```

## Mixture of Experts (MoE) Detail

```
         Input: x[t]
              │
              ▼
       ┌──────────────┐
       │    Router    │
       │  (Linear)    │
       └──────┬───────┘
              │
              ▼
       ┌──────────────┐
       │   Softmax    │
       └──────┬───────┘
              │
              ▼
       ┌──────────────┐
       │   Top-K      │
       │  Selection   │
       │   (K=2)      │
       └──────┬───────┘
              │
    ┌─────────┼─────────┐
    │         │         │
    ▼         ▼         ▼
┌────────┐ ┌────────┐ ... [8 experts total]
│Expert 1│ │Expert 4│
│        │ │        │
│┌──────┐│ │┌──────┐│
││Linear││ ││Linear││
│└───┬──┘│ │└───┬──┘│
│    │   │ │    │   │
│┌───▼──┐│ │┌───▼──┐│
││ GELU ││ ││ GELU ││
│└───┬──┘│ │└───┬──┘│
│    │   │ │    │   │
│┌───▼──┐│ │┌───▼──┐│
││Linear││ ││Linear││
│└───┬──┘│ │└───┬──┘│
└────┼───┘ └────┼───┘
     │          │
     ▼          ▼
   w[1]·o[1] + w[4]·o[4]
              │
              ▼
          Output
```

## Markov Chain Thinking Process

```
Current Context: ["hello", "how"]
                     │
                     ▼
            ┌────────────────┐
            │ Markov Chain   │
            │ Lookup Table   │
            └────────┬───────┘
                     │
         ┌───────────┼───────────┐
         │           │           │
         ▼           ▼           ▼
    Depth 1     Depth 2     Depth 3
    "are"       "you"       "?"
    (p=0.6)     (p=0.4)     (p=0.3)
         │           │           │
         └───────────┼───────────┘
                     ▼
          ┌──────────────────┐
          │ Calculate        │
          │ Confidence       │
          │ = avg(p/depth)   │
          └──────┬───────────┘
                 │
                 ▼ conf=0.43
          ┌──────────────────┐
          │ Adjust Neural    │
          │ Probabilities    │
          │ P'[are] *= 1.43  │
          └──────────────────┘
```

## Generation Pipeline

```
User Input: "Hello"
     │
     ▼
┌─────────────┐
│  Tokenize   │
└─────┬───────┘
      │
      ▼ [72, 101, 108, 108, 111]
┌─────────────────────────────────┐
│  FOR each token position:       │
│                                 │
│  ┌──────────────────────┐       │
│  │ 1. Transformer       │       │
│  │    Forward Pass      │       │
│  └──────┬───────────────┘       │
│         │                       │
│         ▼ logits[vocab_size]    │
│  ┌──────────────────────┐       │
│  │ 2. Apply             │       │
│  │    Temperature       │       │
│  └──────┬───────────────┘       │
│         │                       │
│         ▼                       │
│  ┌──────────────────────┐       │
│  │ 3. Markov Thinking   │       │
│  │    (Adjust Probs)    │       │
│  └──────┬───────────────┘       │
│         │                       │
│         ▼                       │
│  ┌──────────────────────┐       │
│  │ 4. Top-K Filter      │       │
│  └──────┬───────────────┘       │
│         │                       │
│         ▼                       │
│  ┌──────────────────────┐       │
│  │ 5. Sample Next       │       │
│  │    Token             │       │
│  └──────┬───────────────┘       │
│         │                       │
│         ▼                       │
│  Append to sequence             │
│                                 │
└─────────────────────────────────┘
      │
      ▼
Generated: "Hello there"
```

## Data Flow: Training → Inference

```
TRAINING (GPU)
──────────────

Dataset
  │
  ├─→ Tokenize
  │     │
  │     ▼
  │   Token IDs
  │     │
  │     ▼
  ├─→ Embed
  │     │
  │     ▼
  │   Hidden States
  │     │
  │     ├─→ Attention
  │     ├─→ Engram Retrieval
  │     ├─→ Engram Fusion
  │     ├─→ MoE Routing
  │     └─→ Expert Computation
  │         │
  │         ▼
  │       Logits
  │         │
  │         ▼
  │       Loss (Cross-Entropy)
  │         │
  │         ▼
  │       Gradients
  │         │
  │         ▼
  └─→ Update Weights

After Training:
  │
  ├─→ Extract all parameters
  ├─→ Convert to NumPy arrays
  ├─→ Save as .npy files
  └─→ Save config.json

INFERENCE (CPU)
───────────────

Load Weights
  │
  ├─→ config.json → Model structure
  └─→ *.npy files → Parameter values
      │
      ▼
  Reconstruct Model in Lua
      │
      ├─→ Token embeddings
      ├─→ Position embeddings
      ├─→ Engram tables
      ├─→ MoE experts
      └─→ Attention weights
          │
          ▼
  Ready for Generation!
      │
      └─→ Same forward pass
          (but no gradients)
```

## Memory Layout

```
ENGRAM MEMORY TABLES
════════════════════

┌─────────────────────────────┐
│  2-gram Embedding Table     │
│  ┌──────┬────────────────┐  │
│  │ Hash │  Embedding     │  │
│  ├──────┼────────────────┤  │
│  │ 0000 │ [0.12, -0.34...]│ │
│  │ 0001 │ [-0.56, 0.78...]│ │
│  │ ...  │ ...            │  │
│  │ 9999 │ [0.23, -0.45...]│ │
│  └──────┴────────────────┘  │
│  Size: 10,000 × d_model     │
└─────────────────────────────┘

┌─────────────────────────────┐
│  3-gram Embedding Table     │
│  ┌──────┬────────────────┐  │
│  │ Hash │  Embedding     │  │
│  ├──────┼────────────────┤  │
│  │ 0000 │ [0.45, -0.23...]│ │
│  │ 0001 │ [-0.12, 0.67...]│ │
│  │ ...  │ ...            │  │
│  │49999 │ [0.89, -0.34...]│ │
│  └──────┴────────────────┘  │
│  Size: 50,000 × d_model     │
└─────────────────────────────┘

MARKOV CHAIN LOOKUP
═══════════════════

┌─────────────────────────────┐
│  Context → Next Token       │
│  ┌───────────────┬────────┐ │
│  │ Context       │ Next   │ │
│  ├───────────────┼────────┤ │
│  │ [72, 101]     │ 108(4) │ │
│  │ [101, 108]    │ 108(3) │ │
│  │ [108, 108]    │ 111(5) │ │
│  │ ...           │ ...    │ │
│  └───────────────┴────────┘ │
│  Size: Dynamic, grows with  │
│        usage                │
└─────────────────────────────┘
```

This architecture achieves:
- ✅ Efficient memory with Engram (O(1) lookup)
- ✅ Sparse computation with MoE (25% active)
- ✅ Better predictions with Markov thinking
- ✅ Quality control with Top-K sampling
- ✅ CPU-friendly inference in Lua
